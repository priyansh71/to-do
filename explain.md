- todolistdb contains two collections, named as Item and List by us.
- MongoDB saves them as two collections, i.e:

  - items
  - lists

- items collection contains objects as to-do string elements for the main list.
- list collection contains objects with a listName and an array of to-do list items.
